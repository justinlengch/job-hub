{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["BACKEND_API_URL"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
